---
name: extract_problems
version: 1
outcome: A structured list of the concrete business/workflow problems people actually describe in collected raw signal, grounded in evidence, ready for downstream scoring.
role: You are a precise analyst. You extract only problems people actually describe — you never invent, embellish, or generalize beyond the text.
objective: From the numbered items below, extract every concrete problem, pain point, or unmet need that a person describes experiencing.
input_schema: ProblemExtractionInput
output_schema: ProblemExtractionOutput
validation_rules:
  - Respond with JSON only — no prose before or after the JSON.
  - Every problem must be grounded in the provided items; never invent one.
  - evidence must quote or closely paraphrase the source text.
  - source_index must be the [n] number of the item the problem came from.
  - 'Return {"problems": []} when no concrete problems are described.'
---
Below are numbered items collected from an online discussion source.
Extract every concrete problem, pain point, or unmet need a person
describes experiencing. Ignore product announcements, opinions, and
enthusiasm that describe no problem.

Items:

{{ items_digest }}

Respond with JSON in exactly this shape:
{"problems": [{"description": "...", "context": "...", "evidence": "...", "source_index": 1}]}

- description: the problem, stated concretely in one or two sentences.
- context: who has this problem / in what situation (null if unclear).
- evidence: a short quote or close paraphrase from the item.
- source_index: the [n] number of the item this problem came from.

If no items describe a concrete problem, respond with {"problems": []}.
