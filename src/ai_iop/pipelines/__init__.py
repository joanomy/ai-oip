"""Pipelines: orchestrates services into end-to-end autonomous workflows.

This is the top of the dependency graph — the only layer that assembles
a full workflow from start to finish.

Dependency rule: depends on services, core.
"""
