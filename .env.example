# Copy this file to .env and fill in real values.
# .env is git-ignored — never commit real secrets.
#
# This file is currently empty of real settings because the Configuration
# Management module (Milestone 2) hasn't been built yet. It exists now to
# establish the convention: every future setting gets documented here first.

# ENVIRONMENT=development
